<!DOCTYPE html>

<html>
    <head>
        <title>Mood 🌼</title>
        <script src = "{{url_for('static' , filename= 'index.js')}}"></script> 
        <link rel="stylesheet" href = "{{ url_for('static' , filename = 'location.css')}}"> 
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    </head>

    <body>
        <div id = "navbar">
            <div class = "navbarItem" onclick = "navToHome()">
                <a>Home</a>
            </div>
            <div class = "navbarItem" onclick = "navToLoc()">
                <a>Location</a>
            </div>
        </div>

        <div id = "bodyContainer">

            <div id = "headerContainer">
                <h2>Mood User Panel 🌼</h2>
            </div>

            <div id = "locationContainer">

                <div id = "queryContainer">
                    <p>Search for a Location:</p>
                    <input type = "text" id = "location">
                    <button onclick = "getLocation(this)" >Search</button>
                    <div class = "resultContainer">
                        <p>Results:</p>
                        <p id = "locResults" class = "resultsText"></p>
                    </div>
                    <div class = "resultContainer">
                        <p>Coordinates:</p>
                        <p id = "coordsResults" class = "resultsText"></p>
                    </div>

                    <div id = "addLocation" onclick = "addLocation()">
                        <p>Add to List</p>
                    </div>
                
                </div>

                <div id = "listContainer">
                    <p>List of Locations:</p>
                    {%for l in location_list%}
                        <p>{{ l }}</p>
                    {% endfor %}
                </div>
            
            </div>
        </div>
    
    </body>

</html>